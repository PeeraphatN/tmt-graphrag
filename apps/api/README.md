# API App

Target home for the backend application after the repo reorganization.

Planned incoming sources:

- `/src` -> `/apps/api/src`
- `/requirements.txt` -> `/apps/api/requirements.txt`
- selected test coverage from `/scripts` -> `/apps/api/tests`
