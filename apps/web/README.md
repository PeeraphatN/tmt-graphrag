# Web App

Target home for the frontend application after the repo reorganization.

Planned incoming sources:

- `/frontend` -> `/apps/web`
- frontend-only package ownership should live here, not at the repo root
